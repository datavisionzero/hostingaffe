namespace Hostingaffe.Domain.Machines;

/// <summary>
/// The processor architecture (<c>CONTEXT.md</c>, Machine). Closed, and the one
/// hardware fact that is not text: it is what an agent compares, because the
/// image it pulls depends on it.
/// </summary>
public enum Arch
{
    Amd64,
    Arm64,
}
